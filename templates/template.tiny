vstack
    for n in [{"a": 0}, {"a": 1}, {"a": 2}]
        for i in [numbers[n.a]]
            // text numbers[i.ext].ext
            @external {"ext": 1}

    // text [] [numbers[7]][i + -1 - 9].ext
// text numbers[10].ext


// for i in [[3]]
//     text [] i[0]

// vstack
//     text "it is true is " is_true
//     for t in [[{"ext": (12 + 3) * 2}]]
//         @external {"ext": is_true}
