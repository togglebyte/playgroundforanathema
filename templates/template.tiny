zstack
    hstack
        for _ in {{ cols }}
            vstack
                border
                    vstack
                        for val in {{ data }}
                            text '{{ val }} - '
                                for word in {{ words }}
                                    span '{{ word }}'

    position [right: 1, top: 1]
        view "stats"
